# Reasoning Evaluation Test Fixtures

Every file in this directory is synthetic and marked `fixture_only`. The records exist only to verify schemas, loading, evaluation, metrics, serialization, blinding, and failure reporting.

They are **not** the Teammate 2 evaluation benchmark and their metrics are **not** experimental results.
