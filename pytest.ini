[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -ra --strict-config --strict-markers
markers =
    integration: requires live external services or persistent stores
